# MongoDB Memory Server Configuration
# This file configures mongodb-memory-server to avoid download timeouts

# Use specific MongoDB version
MONGOMS_VERSION=7.0.14

# Download URL (use faster mirror if available)
MONGOMS_DOWNLOAD_URL=https://fastdl.mongodb.org/linux/mongodb-linux-x86_64-ubuntu2204-7.0.14.tgz

# Disable automatic download during tests
MONGOMS_DISABLE_POSTINSTALL=1

# System binary path (if MongoDB is installed locally)
# MONGOMS_SYSTEM_BINARY=/usr/bin/mongod

# Download timeout (increase for slow connections)
MONGOMS_DOWNLOAD_TIMEOUT=300000

# Archive name
MONGOMS_ARCHIVE_NAME=mongodb-linux-x86_64-ubuntu2204-7.0.14.tgz

# Debug mode
# MONGOMS_DEBUG=1
